import re

# ret = re.findall(r'\d+', '1-2*(60+(-40.35/5)-(-4*3))')
# print(ret)

# 要匹配的内容太容易和你不想匹配的内容混到一起
# 精准的取到小数，过滤掉小数

# 左边匹配到了，优先显示右边，则左边匹配到的显示为空
# 若 或 的两边同事分组，优先显示，元组形式
ret = re.findall(r'\d+\.\d+|(\d+)', '1-2*(60+(-40.35/5)-(-4*3))')
print(ret)














