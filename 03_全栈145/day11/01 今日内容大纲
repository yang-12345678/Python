主要内容
    1、函数名的应用(第一类对象)
        函数名的命名规范和变量是一样的
        可以作为列表中的元素进行存储

        可以作为参数传递给函数

        可以作为函数的返回值
    2、闭包
        闭包:咋内层函数中访问外层函数的局部变量
        好处：
           1.保护你的变量不受外界影响
           2.可以让变量常驻内存
    3、迭代器
        用来遍历可迭代对象
        可迭代对象：Iterable 里面有__iter__()可以获取迭代器，没有__next__()
        迭代器： Iterator  里面有__iter__()可以获取迭代器，还有__next__()

        迭代器的特点：
            1、只能向前
            2、惰性机制
            3、节省内存
        for循环的内部机制：
            1、首先获取迭代器
            2、使用while循环来获取迭代器
            3、it.__next__()来获取数据
            4.异常处理  try  except

