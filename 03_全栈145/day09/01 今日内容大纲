考试讲解，作业讲解，略

今日主要内容
    1、函数
        函数是对功能或动作的封装
        语法：
            def 函数名()：
                函数体
            调用：
                函数名()
    2、返回值
        1、如果函数什么都不写，不写return，没有返回值，得到的是None
        2、在函数中间或者末尾写return，会终止函数执行，返回的是None
        3、return 值  返回一个值
        4、在函数中可以返回多个返回值，return 值1，值2，值2，，，接收的是元组
    3、参数
        函数执行的时候给函数传递信息
        形参：函数声明位置的变量
        实参：函数调用时给出的值
        传参：把实参传给形参的过程

        1.实参
          1、位置参数，按照形参的位置，给形参传递
          2、关键字参数，按照形参的名字给形参传值
          3、混合参数，既用位置参数，也用关键字参数
        2.形参
          1、位置参数，
          2、默认值参数，放在最后
          3、动态参数