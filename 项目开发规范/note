项目结构（规范）：

bin目录： 只放一个启动脚本

core目录： 核心文件

database(db)：  存放数据

lib：  库  非内置非安装非手写的库（从网上扒）

conf： 配置文件
     用到了某一个值，这个值在程序执行的过程中会被修改，且一旦修改就要修改多处
     这样的值，我们应该单独的写在conf文件夹下面的文件中，来保证以后所有的程序用到这个值
     都需要从这个文件中读取，在需要修改这个值的时候，只需要修改配置文件中的一处就可以了

log： 存放日志